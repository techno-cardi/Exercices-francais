javascript:(async()=>{const root=window.authentification;if(!root){alert('Session Mozaïk introuvable');return}const seen=new WeakSet(),stack=[root],tokens=[];while(stack.length){const o=stack.pop();if(!o||typeof o!=='object'||seen.has(o))continue;seen.add(o);try{if(typeof o.AccessToken==='string'&&o.AccessToken.length>40&&!tokens.includes(o.AccessToken))tokens.push(o.AccessToken)}catch{}let vals=[];try{vals=Object.values(o)}catch{}for(const v of vals)if(v&&typeof v==='object')stack.push(v)}if(!tokens.length){alert('Aucun jeton trouvé');return}const API='https://apiaffaires.mozaikportail.ca';const path='/api/evaluation/matieresOrganisme/732157/anneeCourante/132506/competences';let data=null,last='';for(const token of tokens){try{const r=await fetch(API+path,{headers:{Authorization:'Bearer '+token,'Accept':'application/json, text/plain, */*','Encode-Response':'false'}});last=String(r.status);if(r.ok){data=await r.json();break}}catch(e){last='erreur réseau'}}if(!data){alert('Impossible de lire les compétences S5. Statut '+last);return}function norm(s){return String(s??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim()}function findArray(o,d=0){if(d>6||o==null)return null;if(Array.isArray(o)&&o.some(x=>x&&typeof x==='object')){const ok=o.some(x=>['code','id','valeur','codeCompetence','numero'].some(k=>x[k]!=null));if(ok)return o}if(typeof o==='object')for(const v of Object.values(o)){const r=findArray(v,d+1);if(r)return r}return null}function info(c){const code=c.codeCompetence??c.code??c.valeur??c.numero??c.id;const label=c.description??c.libelle??c.nom??c.titre??c.descriptionLongue??String(code??'');return String(code??'?')+' : '+String(label??'')}const arr=findArray(data)||[];const lines=arr.map(info);console.table(arr);alert('Compétences Français 5 trouvées :\n\n'+(lines.join('\n')||JSON.stringify(data).slice(0,1200))+'\n\nLecture seulement, aucune donnée modifiée.');})()